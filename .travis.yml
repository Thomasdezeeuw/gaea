sudo: false
cache: cargo
os:
  - linux
  - osx
env:
  - RUST_BACKTRACE=1
matrix:
  fast_finish: true
  allow_failures:
    - os: osx
language: rust
rust:
  - stable
  - beta
  - nightly
script:
  - cargo --version
  - rustc --version
  - cargo build --verbose
  - cargo test --verbose
